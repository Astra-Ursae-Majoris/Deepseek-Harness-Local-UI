# Release Notes — v0.1.3

**DeepSeek Harness 本地桌面版（Deepseek-Harness-Local-UI）首个公开发布**

---

## 🎉 发布亮点

DeepSeek Harness 官方 Web GUI 的 Windows 桌面发行版：**免命令行、免浏览器折腾**，点按钮即可启动 / 停止 / 重启本地 DSH 服务，并附带修改版 GUI 的对话大纲导航与回退 / 重新生成能力。

## 📦 下载

| 文件 | 说明 |
| --- | --- |
| `DSH-Desktop-0.1.3-portable.exe` | **便携版（推荐）**：免安装，双击即用，约 85 MB |

> 完整源码（含 `harness/` 官方源码修改版）见仓库 **Code → Download ZIP**，或 `git clone`。

## ✨ 新特性

### 🖥 桌面 EXE 壳

- **原生窗口**加载本地 Web GUI（仅 `127.0.0.1` 白名单）；
- **服务管家**：一键 启动 / 停止 / 重启 DSH 服务，实时状态显示（运行中 / 已停止 / 异常）；
- **托盘常驻**：「保持服务后台运行并退出」后服务进程独立存活，下次打开秒进；
- **右键手势**：按住右键左右拖动 = 收起 / 展开侧边栏（`Alt+← / →` 同效）；
- **快捷键屏蔽**：不再被 Ctrl+T / Ctrl+W 等浏览器快捷键打扰；
- **文件拖放**：拖入图片等文件获得真实文件路径；
- **模型 / API 密钥管理窗口**：可视化配置（应用不存储密钥）；
- **安装目录自动检测**：`DSH_HOME` → 同级 `harness/` → 常见位置 → 手动选择；
- 窗口状态记忆、单实例锁、完整安全策略（沙箱 / 权限拒绝 / 仅本机）。

### 🔍 修改版 Web GUI 增强

- **对话大纲导航栏**：右侧常驻目录，完整加载全部轮次（首句摘要），点击定位 + 高亮提示；窗口过窄自动收起；
- **回退到这一步**：任意用户消息一键 fork 分支并打开子会话；
- **重新生成**：fork 后自动重发原消息；
- 新增 `session.outline` 服务端 RPC（逐轮摘要，独立于窗口加载）。

## 🚀 快速开始

```bash
# 1. 准备环境（Windows 10/11 x64）
#    Node.js ≥ 22.19 + pnpm ≥ 9

# 2. 安装 DSH 依赖（只需一次）
cd harness
pnpm install

# 3. 配置 API 密钥
echo DEEPSEEK_API_KEY=你的密钥 > .env

# 4. 回到仓库根目录，双击 release/DSH-Desktop-0.1.3-portable.exe
#    欢迎页点「🚀 启动服务」→ 自动进入聊天界面
```

> 💡 也可以不用 EXE：`pnpm dsh web --host 127.0.0.1 --port 3080` 后用浏览器访问。

## 🧩 与上游的关系

- 基于 [DeepSeek Harness](https://github.com/deepseek-ai/deepseek-harness) 官方提交 `f98a57e` 二次开发；
- 官方源码占比 ≈ **99.75%**（≈ 742,000 行），本仓库修改净增 ≈ **0.25%**（≈ 1,845 行），桌面壳为独立新增代码；
- 全部代码遵循 **MIT License**：上游版权 © 2026 DeepSeek，修改与桌面壳版权 © 2026 Astra-Ursae-Majoris；
- 详细占比、改动清单与法律声明见仓库 [NOTICE.md](NOTICE.md)。

## 🔒 安全说明

- 仅允许加载 `http://127.0.0.1:<端口>`，外部链接交给系统浏览器；
- 渲染进程沙箱 + 上下文隔离；摄像头 / 麦克风等权限一律拒绝；
- 服务仅绑定 127.0.0.1，不对局域网开放；
- API 密钥不落盘、不上传，仅读取环境变量 / `.env`。

## ❓ 常见问题

- **找不到 DSH 安装目录？** → 欢迎页「更改 DSH 安装位置」选择 `harness/` 目录；
- **端口被占用？** → 点「重启服务」自动接管；
- **服务重启后对话会丢吗？** → 不会，数据持久化在会话存储中；
- **需要联网吗？** → 需要 DeepSeek API（对话推理），服务本身全在本机。

## ⚖️ 免责声明

本项目与 DeepSeek 公司无任何隶属、背书或合作关系；按 MIT 协议以「AS IS」提供，不附带任何担保。

---

**欢迎 Star ⭐ / Issue / Fork。**
